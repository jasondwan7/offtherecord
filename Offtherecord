<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Off the Record — Where the night belongs to the moment.</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0a0a0f;
      --card:#101018;
      --ink:#f2f2f7;
      --muted:#a8a8b3;
      --accent:#4aa3ff;
      --accent-2:#ff4ad8;
      --glow: 0 0 24px rgba(74,163,255,.45), 0 0 48px rgba(74,163,255,.25);
    }
    *{box-sizing:border-box}
    body{margin:0;background:var(--bg);color:var(--ink);font-family:Inter,sans-serif;line-height:1.55}

    /* NAV */
    .nav{
      position:sticky;top:0;z-index:999;
      backdrop-filter: blur(10px);
      background:linear-gradient(to bottom, rgba(10,10,15,.9), rgba(10,10,15,.5));
      border-bottom:1px solid rgba(255,255,255,.06);
    }
    .nav .inner{display:flex;align-items:center;justify-content:space-between;padding:14px 20px}
    .brand{display:flex;align-items:center;gap:10px;font-weight:800;letter-spacing:.5px}
    .brand .logo{height:40px;width:auto;display:block}
    .brand-text{white-space:nowrap}
    .menu{display:flex;gap:18px;flex-wrap:wrap}
    .menu a{color:var(--ink);opacity:.85;text-decoration:none}
    .menu a:hover{opacity:1;text-decoration:none}

    /* HERO */
    .hero{min-height:80vh;display:flex;align-items:center;justify-content:center;text-align:center}
    .headline{font-size:clamp(32px, 5vw, 72px);font-weight:800;margin:0 0 10px}
    .tagline{font-size:clamp(16px,2.2vw,22px);color:var(--muted);margin:0 0 24px}

    /* Buttons */
    .btn{
      background:linear-gradient(180deg, rgba(74,163,255,.95), rgba(74,163,255,.85));
      color:white;border:0;border-radius:10px;padding:12px 18px;font-weight:700;
      box-shadow:var(--glow);cursor:pointer;transition:transform .08s ease;
      display:inline-block;
    }
    .btn.secondary{background:transparent;border:1px solid rgba(255,255,255,.18);color:var(--ink)}
    .btn:active{transform:translateY(1px)}
  </style>
</head>
<body>
  <!-- NAV -->
  <nav class="nav">
    <div class="inner">
      <div class="brand">
        <!-- Replace otr-logo.png with your actual file name if different -->
        <img src="otr-logo.png" alt="Off the Record Logo" class="logo">
        <span class="brand-text">Off the Record</span>
      </div>
      <div class="menu">
        <a href="#home">Home</a>
        <a href="#about">About</a>
        <a href="#events">Events</a>
        <a href="#tickets">Tickets</a>
        <a href="#newsletter">Newsletter</a>
        <a href="#contact">Contact</a>
      </div>
    </div>
  </nav>

  <!-- HERO -->
  <header id="home" class="hero">
    <div>
      <h1 class="headline">Off the Record</h1>
      <p class="tagline">Where the night belongs to the moment.</p>
      <a href="#tickets" class="btn">Get Tickets</a>
    </div>
  </header>

  <!-- FOOTER -->
  <footer style="padding:20px;text-align:center;color:var(--muted)">
    © <span id="year"></span> Off the Record
  </footer>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
